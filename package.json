{
  "name": "the-faintest-ink",
  "version": "1.0.0",
  "description": "The faintest ink is more powerful than the strongest memory",
  "main": "README.md",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "serve": "vitepress serve docs --port 3001",
    "deploy": "rm -rf ./**/.DS_Store && cloudbase hosting:deploy dist /notes -e elvinn-1aa07a"
  },
  "keywords": [
    "note"
  ],
  "author": "elvin.pjw@gmail.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/elvinn/the-faintest-ink.git"
  },
  "devDependencies": {
    "@cloudbase/cli": "^0.8.1",
    "stylus": "^0.56.0",
    "vitepress": "^0.20.10"
  },
  "dependencies": {
    "moment": "^2.29.1"
  }
}
